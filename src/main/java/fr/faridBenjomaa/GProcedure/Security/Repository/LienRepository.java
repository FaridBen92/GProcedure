package fr.faridBenjomaa.GProcedure.Security.Repository;

import fr.faridBenjomaa.GProcedure.Security.Entity.Liens;
import org.springframework.data.jpa.repository.JpaRepository;

public interface LienRepository extends JpaRepository<Liens, Long> {
}
